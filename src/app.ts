// import { GraphItem } from './data-types'
import { graphData as _graphData } from './mock-data'

export class App {
  graphData = _graphData

  constructor() {
    // console.log(this.graphData)
  }
}
